SpringMVCHibernateSample
========================

Sample for Spring MVC+Hibernate
This is a sample of Spring MVC + Hibernate, use Oracle Database, the function is add a new user into the database, but will store the duplicate ones
